<!-- GENERATED BY AKR — DO NOT EDIT
     source-graph: sha256:368facfd728d84fe1099fcd74f3c02521f55e72c93f5110888359dd79203484e
     tool: akr 0.3.1
-->

# Current state — lege-ecosystem

What the project believes right now: live terms, constraints, policies and requirements, and what has been found or assessed. Decisions have their own view, `DECISION-HISTORY.md`.

## Terms

_(none)_

## Constraints

_(none)_

## Policies

### 2026-07-19 implementation update

`proposed` · `@lege-ecosystem.policy.implementation-update/1` · scope `all`

- Sections 1 and 2 are implemented. Tier-2 now models per-resolution precinct
  grids, per-precinct tag trees/code-block routing, all five progression orders,
  and SOP/EPH. The degenerate irreversible 9/7 one-sample phase is fixed before
  relaxing the decomposition bound.
- The Armenia obj8 repro was not precinct-partitioned: after SOP/EPH parsing it
  exposed code-block style `0x3e`. Annex B.10.7.2 terminated-segment lengths and
  Annex D RESET, TERMALL, VSC, predictable termination, and segmentation
  symbols are now implemented. The actual 9/7 output differs from OpenJPEG by
  max 1 (mean 0.00053).
- The Section 3 diagnosis was incomplete. Its extracted 150-tile PDF stream is
  structurally complete according to OpenJPEG, and zero-byte contributions are
  valid with Annex D.4.1 synthetic end bytes. That syntax is now accepted, but
  the repro then exposes a corrupt 1,349-byte tile tail. A salvage prototype
  corrupted the entire affected 128×128 tile, so strict failure remains.
- Section 4 remains unresolved. The same strict no-tail policy is retained
  because both measured salvage attempts produced visible corruption.

### 2026-07-22 (later) update — Section 2 RESOLVED, and sub-8-bit precision added

`proposed` · `@lege-ecosystem.policy.later-update-section-2-resolved-and-sub-8-bit/1` · scope `all`

- **Section 2 was a real DWT bug, and this is it.** `forward_97_vertical_in_place` /
  `inverse_97_vertical_in_place` scaled a *one-row* resolution by INV_K / K.
  The pair round-trips through our own encoder, so every test passed, but it
  does not match conformant streams: OpenJPEG returns from both
  `opj_dwt_encode_and_deinterleave_v_real` and `opj_v8dwt_decode` on height 1
  without touching the data, and **our own horizontal 1-D path already treated
  the case as an identity** — the two disagreed. This is exactly the drift §2
  and §5-trap-1 recorded when the decomposition bound was lifted: a codestream
  that signals more levels than its dimensions need passes through several 1x1
  levels, so its DC comes out multiplied by K^n while the AC stays
  pixel-perfect. On the 884x1344 / 17-resolution repro a fit against OpenJPEG
  gave `ours = 1.002 * opj + 194` — a pure DC offset, K^5 = 2.8158.
  With the identity restored the dimension-derived bound has nothing left to
  reject; `max_dwt_decompositions` is gone and the only limit is the spec's own
  `NL <= 32`. Repro (Howard R. Turner, *Science in Medieval Islam* obj7):
  max abs diff 5, mean 0.147, 3062 of 1,188,096 px over 2 — the 9/7 float floor
  at 11 real levels rather than the usual 5, diffuse and edge-concentrated.
  End-to-end 0.2424 silent-blank(codec) -> 0.0014.
- **Sub-8-bit sample precision (1/2/4-bit) is implemented.** The SIZ gate
  accepted only 8..=16 bits; bitonal scans are 1-bit and were the largest
  remaining census class. Nothing in the pipeline needed a special case.
  Verified bit-exact vs OpenJPEG (max abs diff 0) on pdfbox/3246 obj3.
  **NOTE for future verification work:** the CLI's PNG writers used to clamp
  raw samples to 0..255, which wrote a 1-bit page as 0/1 and *clipped* 16-bit
  components instead of scaling them. They now scale by component precision.
- **Renderer-side caveat (not a codec issue):** pdf-renderer widens sub-8-bit
  samples with PDFium's `src << (8 - prec)`, not full-range scaling, because
  that is what PDFium's `CJpx_Decoder::Decode` does and it is what the corpus
  grades against (exactly 0.00000 on pdfbox/3246 and 4326; full-range leaves
  them at 0.81-0.99). Do not "fix" that to match OpenJPEG's writers.

### 28. Renderer integration policy

`proposed` · `@lege-ecosystem.policy.renderer-integration-policy/1` · scope `all`

The PDF renderer is already document-parallel. Hidden codec threading could reduce whole-document throughput through oversubscription.

### 29. Replace eager packet-position sorting where worthwhile

`proposed` · `@lege-ecosystem.policy.replace-eager-packet-position-sorting-where/1` · scope `all`

`build_packet_positions()` currently constructs every packet position, pairs it with a five-element sort key, sorts the full vector, and then discards the keys.

### 31. Reuse per-packet contribution storage

`proposed` · `@lege-ecosystem.policy.reuse-per-packet-contribution-storage/1` · scope `all`

`push_tile_part()` creates a fresh `Vec<Contribution>` for every packet.

### The census / extraction tool (your main instrument)

`proposed` · `@lege-ecosystem.policy.the-census-extraction-tool-your-main-instrument/1` · scope `all`

`pdf-renderer/crates/pdf-cli/examples/jpxcensus.rs` extracts every `/JPXDecode`
stream from a list of PDFs, decodes each with `jp2lam`, and tallies normalized
error signatures. Set `JPXDUMPDIR` to also dump one representative failing
stream per signature to disk (as `.jp2`) for `opj_dump`/`opj_decompress`.

### 4. `TPsot==TNsot` non-conformant tile-parts — ~2 files  [RESOLVED 2026-07-22 — the assembly-bug diagnosis in this section is WRONG; real cause was a missing `inalign` byte-realignment. See the 2026-07-22 update near the top.]

`proposed` · `@lege-ecosystem.policy.tpsot-tnsot-non-conformant-tile-parts-2-files/1` · scope `all`

- **Error:** `tile-part contains data after the tile's packet sequence`
- **Repro:** `Prussian-Line-Infantry-1792-1815.pdf` obj15 (1208×1638, 3-comp,
  csty=0, no precincts). OpenJPEG decodes it, emitting
  `[WARNING] Non conformant codestream TPsot==TNsot.`
- **Reject site:** `src/decode/t2.rs` `push_tile_part` ~line 171-176 — after the
  full packet sequence is read, leftover bytes trigger the error.
- **Diagnosis & TRAP:** the leftover bytes are **not** padding — they are a
  genuine tile-part chunk that our tile-part assembly misplaces because the
  stream sets `TPsot == TNsot` (an off-by-one tile-part index). I tried the
  naive fix (break out of the loop, ignore the tail); it decodes but **corrupts
  a 256-row code-block band** (rows 768-1023 of 1638; measured 1.4% of pixels
  >30 off, a visible stripe). That is a silent mis-decode — worse than the clean
  blank — so the strict failure is kept deliberately (see the NOTE comment now
  in `push_tile_part`).
- **Recommended approach:** fix the **tile-part boundary / ordering** for the
  non-conformant `TPsot==TNsot` case in `src/decode/mod.rs`
  `tile_part_indices_by_tile` (the TNsot-advisory logic) so every tile-part's
  payload is assigned to the correct tile and concatenated in the right order —
  then there is no "trailing" data. Compare against OpenJPEG's tolerant
  tile-part handling (`opj_j2k_read_sot` / the `TPsot==TNsot` warning path).
  Verify the Prussian stream reaches max diff ≤ 2 with no corrupted band.

### 13. GPU path: useful, but only with renderer integration

`proposed` · `@lege-ecosystem-perf.policy.gpu-path-useful-but-only-with-renderer/1` · scope `all`

NVIDIA’s current nvJPEG2000 library offloads every decode stage except Tier-2 to Pascal-or-newer GPUs, supports 5/3 and 9/7, multiple tiles, up to four components, common chroma subsampling, resolution/tile decode, partial decode, RGB output controls, and reusable decode state. On an RTX 4060, it is a viable experimental control or optional backend.

### 11. Parallelism policy for a PDF renderer

`proposed` · `@lege-ecosystem-perf.policy.parallelism-policy-for-a-pdf-renderer/1` · scope `all`

The decoder cannot choose thread count in isolation. The renderer already runs pages/images concurrently. Nested unrestricted Rayon work can reduce document throughput and increase tail latency.

### Phase 5 — true windowed/line-based decode

`proposed` · `@lege-ecosystem-perf.policy.phase-5-true-windowed-line-based-decode/1` · scope `all`

1. Windowed subband allocation.
2. Row- and column-limited IDWT.
3. Strip/row callback finalization into the renderer or GPU upload.
4. Parsed-plan cache.
5. ROI coverage threshold policy.

### 16. Recommended renderer API and policy

`proposed` · `@lege-ecosystem-perf.policy.recommended-renderer-api-and-policy/1` · scope `all`

A renderer-specific constructor should make the fast path explicit and difficult to bypass:

### 5.1 Scratch allocation per parallel row

`proposed` · `@lege-ecosystem-perf.policy.scratch-allocation-per-parallel-row/1` · scope `all`

At `src/dwt/irrev97.rs:871–877`, every row handled by `par_chunks_mut()` allocates:

## Requirements

### 2. Degenerate 1-sample DWT split — ~8 files  [RESOLVED 2026-07-22 — root cause was a K/INV_K scaling on a one-row resolution in the *vertical* pass; see the update near the top. The "relaxing the bound drifts" note below is accurate but the cause is now fixed.]

`proposed` · `@lege-ecosystem.requirement.degenerate-1-sample-dwt-split-8-files-resolved/1` · scope `all`

- **Error:** `COD decomposition levels N exceed DWT limit N for image
  dimensions WxH`
- **Repro:** `Neo-Confucianism-in-History.pdf` obj9 — a **176×16** tile
  declaring **5** decomposition levels (numresolutions=6). OpenJPEG decodes it.
- **Reject site:** `src/j2k/decode_markers.rs` `max_dwt_decompositions`
  (~line 577) returns `ceil(log2(min_dim))`; for min_dim=16 that's 4, so 5 is
  rejected at line 462-465.
- **Diagnosis (IMPORTANT — this is a real DWT bug, not just a bound):** the
  extra level splits an axis that is already down to **1 sample** (16 → 8 → 4 →
  2 → 1, then a 5th level on the height axis operates on a 1-sample band). I
  measured that if you merely relax the bound to `ceil(log2(max_dim))` and let
  it decode, `jp2lam`'s **inverse DWT drifts ~16/255 (mean), max 25** vs
  OpenJPEG on that axis — i.e. it silently mis-decodes. So the bound is
  currently kept strict *on purpose* (a clean B3-flagged failure beats a wrong
  image — see §5).
- **Recommended approach:** fix the inverse DWT (`src/dwt/`, `irrev97.rs` /
  `rev53.rs` and the 2D driver `inverse_97_2d_in_place_at`) to correctly handle
  a lifting step on a length-1 (and length-0) axis — it must be a pass-through
  that matches OpenJPEG's `opj_dwt_decode` boundary handling. **Then** relax
  `max_dwt_decompositions` to `ceil(log2(max_dim))` (the larger dimension) and
  re-verify the 176×16 stream is now max diff ≤ 2. Do the DWT fix first; the
  bound relaxation without it re-introduces the drift.
- **Verification:** decode the dumped `failjpx_…decompositionlevels….jp2` and
  diff vs OpenJPEG — must drop from mean ~16 to ≤ ~1.

### 0. Orientation & environment

`proposed` · `@lege-ecosystem.requirement.orientation-environment/1` · scope `all`

- **Repo:** `D:\Rust-projects\jp2lam` on Windows; the same disk boots Linux
  where the path is `/mnt/Samsung980_1TB/Rust-projects/jp2lam`. Dual-boot, one
  drive: **`D:\` on Windows == `/mnt/Samsung980_1TB` on Linux.** The corpus
  under `D:\Pol was right again\…` is the Linux `/mnt/Samsung980_1TB/Pol was
  right again/…` in the sweep CSV.
- **Build / test:** `cargo test` (247+ lib tests; a few `#[ignore]`d ones need
  archive.org sample files). CLI decoder: `cargo build --features cli` →
  `target/debug/jp2lam.exe decode <in.jp2> <out.png>`.
- **Rust 1.95 pin** (edition 2024). The crate is also a path-dependency of
  `pdf-renderer`; after changing it, `cargo build --workspace` there must stay
  green (it does today).
- **Oracle = OpenJPEG.** `opj_decompress -i x.jp2 -o x.png` and
  `opj_dump -i x.jp2` are on PATH. **Every fix must be verified bit-exact vs
  OpenJPEG**: 5/3 reversible transform → max abs diff **0**; 9/7 irreversible →
  max abs diff **≤ ~2** (float rounding). A larger diff means the fix is wrong —
  see the two traps in §5.

### 1. Precincts / SOP / EPH — ~10 files (LARGEST remaining class)

`proposed` · `@lege-ecosystem.requirement.precincts-sop-eph-10-files-largest-remaining/1` · scope `all`

- **Error:** `unsupported packet syntax: precinct partitioning, SOP markers,
  and EPH markers are not implemented`
- **Repro:** `The-Conversion-Of-Armenia-To-The-Christian-Faith.pdf` obj8
  (`D:\Pol was right again\…\Armenia…`). OpenJPEG decodes it.
- **Reject site:** `src/j2k/decode_markers.rs` ~line 475 (`validate_decoder_scope`):
  `if cod.uses_precincts || cod.sop_markers || cod.eph_markers { … }`. The COD
  `Scod` bits are already parsed (`uses_precincts`, `sop_markers`, `eph_markers`
  at lines 335-337; `precinct_sizes: Vec<PrecinctSize>` is carried on
  `CodSegment`).
- **Why it's the big one:** the Tier-2 packet iterator in `src/decode/t2.rs`
  currently assumes **P=1** (one precinct per resolution) — `packet_axis_order`
  iterates only (layer, resolution, component). Precincts add a fourth axis: a
  packet exists per (layer, resolution, component, **precinct**), and the number
  of precincts per resolution is derived from the `PPx/PPy` sizes and the
  resolution's coordinates. This is the load-bearing change.
- **Recommended approach:**
  1. In `t2.rs`, generalize the packet odometer to include the precinct index
     as the innermost spatial axis (Annex B.12 order within a resolution).
     Compute per-resolution precinct counts from `precinct_sizes` and the
     tile-component reference grid (Annex B.6): `numprecincts_x =
     ceil(trx1/2^PPx) - floor(trx0/2^PPx)` etc.
  2. Route each packet's code-block contributions to the code-blocks of its
     precinct only (precincts partition each subband into rectangles; a
     code-block belongs to exactly one precinct). Today `band_lookup` maps
     (component,resolution)→bands; you'll need band→precinct→code-block routing.
  3. **SOP** (`0xFF91`, before a packet) and **EPH** (`0xFF92`, after the packet
     header) are optional delimiters — detect and skip them at the right points
     in `push_tile_part` / the packet header reader. They don't change the data,
     only frame it; but if `sop_markers`/`eph_markers` are set you MUST consume
     them or the byte cursor desyncs.
  4. Port against OpenJPEG's `opj_t2_decode_packets` / `opj_pi_*` for the
     iteration order and precinct math — that's the reference the corpus was
     produced against.
- **Verification:** the Armenia streams; expect 9/7 max diff ≤ 2. Add a focused
  unit test with a precinct-partitioned fixture (`opj_compress -c [64,64]` makes
  one). Re-run the census; this should clear ~10 files.

### 8. Reduced reconstruction geometry

`proposed` · `@lege-ecosystem.requirement.reduced-reconstruction-geometry/1` · scope `all`

Reconstruction must use the dimensions and reference-grid phase associated with the selected resolution, not simply divide the full dimensions by a power of two.

### 2026-07-22 update — Sections 3 and 4 RESOLVED (the §4 diagnosis below was WRONG)

`proposed` · `@lege-ecosystem.requirement.update-sections-3-and-4-resolved-the-4/1` · scope `all`

- **Section 4 was NOT a tile-part assembly bug.** That diagnosis is disproven:
  the Prussian obj15 repro has 35 tiles with an identical 6-part/TNsot=5
  structure, yet only tile 16 failed — an assembly/ordering fault would break
  all 35. The real cause is that the Tier-2 packet-header bit reader
  (`PacketBioReader`) was missing the terminating **byte-realignment** (ISO/IEC
  15444-1 Annex B.10.1 = OpenJPEG `opj_bio_inalign`): when a packet header's
  last consumed byte is `0xFF`, the following bit-stuff byte belongs to the
  header and must be consumed before the body. Tile 16's R3C0 header ends
  `0xFF 0x00`; the old reader sliced the body one byte early at the stuff byte
  and desynced every later packet of that tile — which surfaced as the "leftover
  bytes after the packet sequence" error, and (under naive tail-tolerance) as
  the exact documented 256-row corruption stripe. Fix: `PacketBioReader::inalign()`
  called after every packet-header parse (`src/decode/t2.rs`). Verified: the
  corrupt rows 768-1023 go from max 255 / 75134 px >30 to max 3 / 0 px >30;
  overall max 4 (MCT float floor); end-to-end all pages `degraded=0`.
- **Section 3's named repros were the same `inalign` desync, not truncation.**
  Profit-Over-People obj4 now decodes bit-exact (5/3 max 0); the Imre Nagy
  streams decode within the MCT float floor. **Genuine** stream truncation is
  kept strict — a raw-truncated stream makes OpenJPEG itself error ("Tile part
  length inconsistent with stream length"), so a clean failure is correct there.
- **Also landed 2026-07-22:** 4:2:0 sYCC chroma-subsampling (narrowly gated;
  per-component Annex B.2 geometry + `sycc420_to_rgb`-matching upsample), and
  colr METH 3/4 (any-ICC / vendor colour, inferring the space from the ICC data
  space or component count; ambiguous 2-component rejected cleanly) + palette
  channel-count reconciliation. All verified bit-exact vs OpenJPEG.

### 1. What “large render” means for this problem

`proposed` · `@lege-ecosystem-perf.requirement.what-large-render-means-for-this-problem/1` · scope `all`

There are three sizes that must be recorded separately:

## Observations

### jp2lam lossy quality trails JPEG XL/BPG and collapses with resolution (codec-headsup, 2026-08-11)

`verified` · `@lege-ecosystem.encode-quality.headsup-measurement-2026-08-11/1` · scope `path "lege-codecs/jp2lam/src/dwt/pcrd.rs"`, `path "lege-codecs/jp2lam/src/encode/**"`, `path "lege-codecs/jp2lam/src/plan/**"`

A standalone Rust head-to-head (codec-headsup, at /mnt/Samsung980_1TB/Rust-projects/codec-headsup) compared jp2lam JPEG 2000, libjxl JPEG XL, and bpg-rs BPG on phone photos (a balanced 100x ~50MP + 100x ~12MP sample), grading DECODED RGB with the butteraugli and ssimulacra2 crates. At a fixed operating point (JXL cjxl -d 1.0 -e 9; BPG best qp12; jp2lam RateControl::Quality(98) via Preset::PhotoHigh), across the first 35 completed images jp2lam q98 reached a mean butteraugli of ~4.2 (median ~3.8) versus ~1.3 for JPEG XL and ~1.4 for BPG, while being LARGER than JPEG XL in 34/35 cases (mean size jp2/bpg 0.87, jxl/bpg 0.60, jxl/jp2 0.73).

Quality was strongly resolution-dependent at the SAME quality setting: jp2lam q98 achieved butteraugli ~2 on ~12MP images but ~5-10 on ~50MP images (e.g. 20260105_095336, 50MP: jp2 butteraugli 10.2 at 4.93MB vs jxl butteraugli 1.31 at 4.22MB), and mean ssimulacra2 fell from ~88 on 12MP to ~82 on 50MP. A fixed jp2lam quality therefore does not hold a fixed perceptual quality, and jp2lam's quality-per-byte on this photographic set is below both next-gen codecs. Separately, decoding a 50MP JP2 exceeded the default DecodeLimits working-set cap (estimated ~3.0 GB vs the 1 GiB default), aborting decode until the limit was raised.

### Decode flamegraph baseline (lear q75 RGB, P-cores)

`verified` · `@lege-ecosystem-perf.observation.decode-flamegraph-lear-2026-08-06/1`

Flamegraph of the session_packed RGB decode of lear.png (4485x2791,
irreversible 9/7, q75) at 4 workers pinned to physical P-cores
(taskset -c 0,2,4,6), event cpu_core/cycles/u, dwarf call-graph, warm
process. Wall median ~280 ms (~43 MP/s). Self-time leaf hotspots (share
of cpu_core cycles across 4 threads):
- 9/7 inverse DWT ~25%: apply_lift_wide 14.2% (already wide-SIMD),
  inverse_97_1d_with_scratch_phase 4.8%, plus _mm_mul_ps/_mm_add_ps.
- Tier-1/MQ ~22%: MqDecoder::decode_with_ctx 7.3%,
  FlagGrid::neighbour_mask_from_word 4.8%, renormd 2.3%,
  lps_exchange 1.8%, sigpass ~2%.
- Finalize/pack/color ~15% (scalar): <f32>::clamp 4.7%,
  finalize_centered_f32_u8 4.5%, fuse_pack_f32_into 2.6%,
  store_rgb_layout 1.5%, ict_rgb_f32 1.3%.
- __memcpy_avx_unaligned_erms 6.1% (DWT band gather/scatter + staging).
Note: decode_benchmark's bytes_hash closure (~4.7%) is measurement-only
overhead, not decoder work. Artifacts:
lege-codecs/jp2lam/.agent/scratch/flamegraphs/jp2lam-lear-2026-08-06/.
Method transferred from jpegXL-rs: P-cores only + cpu_core event avoids
hybrid-Intel cpu_atom stack pollution.

## Assessments

### jp2lam photographic quality limited by MSE-only, disabled-perceptual, size-matched rate control

`verified` · `@lege-ecosystem.encode-quality.root-cause/1` · scope `path "lege-codecs/jp2lam/src/dwt/pcrd.rs"`, `path "lege-codecs/jp2lam/src/encode/backend/native/rate.rs"`, `path "lege-codecs/jp2lam/src/plan/**"`

jp2lam's lossy photographic quality trails JPEG XL/BPG for structural reasons in the rate-control path, not a coding-correctness bug. The active PCRD selection optimizes measured mean-squared error with no perceptual weighting; the perceptual machinery that exists is disconnected/rolled back; and the quality-to-lambda mapping is calibrated to reproduce old FILE SIZES from one 12MP image, with a resolution term that actively degrades large images. These compound into the measured resolution-dependent quality collapse.

- `#mse-not-perceptual` — The active rate path (src/encode/backend/native/rate.rs raw_records_for_passes) builds RD curves from a measured Annex-J delta-MSE (pass.mse_numerator * quant_step^2 * subband synthesis norm * component weight) and select_for_quality picks passes at an MSE-domain lambda. curves_from_tier1_layout explicitly discards the perceptual inputs (`let _ = (quality, contrast_mask, taubman_weights);`), so selection is MSE-optimal with zero perceptual weighting. butteraugli and ssimulacra2 penalize MSE-optimal allocation, so the codec is optimizing the wrong objective for photographic quality.
- `#perceptual-path-disabled` — A perceptual model exists but is not wired into selection: src/perceptual/ (contrast_mask.rs, taubman_masking.rs), and pcrd.rs functions apply_contrast_masking_to_delta and band_distortion_bias, plus the Taubman2000 distortion model, are present yet unused by the active curves_from_tier1_layout path. rate.rs documents that the real Annex-J delta-MSE + perceptual weighting effort (P1.3) was ROLLED BACK 'pending lambda recalibration'. So the perceptual rate control is stubbed, not merely missing.
- `#resolution-lambda-backwards` — pcrd.rs quality_to_lambda multiplies the RD threshold by resolution_factor = (pixel_count / 12_000_000)^0.35, ~1.65x at 50MP. A higher lambda truncates MORE coding passes, so the largest, most detailed images are quantized/truncated hardest - the opposite of holding constant visual quality. This directly explains the measured 12MP->50MP butteraugli collapse (butteraugli ~2 -> ~5-10 at q98).
- `#size-matched-single-image-calibration` — The whole quality curve is calibrated to REPRODUCE FILE SIZE, not perceptual quality: quality_to_lambda anchors were 'bisected on lear.png' (a single 12MP image) 'so each quality reproduces the file size of the previous heuristic-model curve within ~1%', and plan/derive.rs quality_step_scaler / document_step_scale were tuned on 300-dpi text pages. There is no butteraugli/ssimulacra2-anchored calibration and no cross-resolution or multi-image validation.
- `#uniform-quantization-no-csf` — plan/derive.rs apply_quality_step_scaling multiplies every subband's ISO expounded step by a single quality-only scalar (quality_step_scaler), with no CSF/visual frequency weighting per subband. Bit emphasis across subbands is thus content- and perception-blind before PCRD, and the only band shaping (band_distortion_bias LL 1.2 / HH 0.82) lives in the unused perceptual path.

**supported_by** `@lege-ecosystem.encode-quality.headsup-measurement-2026-08-11/1`

### Fused-pack SIMD is performance-neutral (memory/codegen-bound)

`verified` · `@lege-ecosystem-perf.assessment.fused-pack-simd-neutral/1`

Hypothesis from the 2026-08-06 decode flamegraph: the ~15% of cycles in
the scalar fused pack (finalize_centered_f32_u8 + <f32>::clamp +
ict_rgb_f32 + store_rgb_layout inside fuse_pack_f32_into) could be cut
with an 8-wide wide::f32x8 finalize/ICT. Implemented a bit-exact
f32x8 path for the tight parallel branch (output hash unchanged at
c1698aa01d9a325e; 339 lib tests pass). Interleaved A/B on lear q75 RGB
(4 P-core workers, two prebuilt binaries alternated to cancel thermal
drift) showed no difference: baseline 278.3-281.4 ms vs SIMD
278.5-281.3 ms. The "after" flamegraph cycle shares for finalize/clamp
were unchanged, indicating LLVM already auto-vectorizes the scalar
per-pixel loop under fat LTO, and the pack is memory-bandwidth-bound
(reads 3x ~37 MP f32 planes, writes ~37 MP x3 bytes). The change was
reverted as neutral-but-more-complex. Transferable lesson (from
jpegXL-rs): do layout/memory-traffic work before ISA SIMD; SIMD only
accelerates arithmetic between large allocations and copies.
Next real targets by the flamegraph: 9/7 inverse DWT vertical memory
traffic (apply_lift_wide 14% + band gather/scatter memcpy) and Tier-1
MQ/flag hot loop, not the pack arithmetic.

## Evidence

### Embedded About loopback delivery passes Freya suite

`verified` · `@lege-ecosystem.evidence.about-loopback-freya-tests/1`

All 38 Freya tests passed, including end-to-end loopback readability and exact embedded document allowlisting.

**Verifies**

- `completed` `@lege-ecosystem.work.about-image-pdf-agent-tool/1` — check `about-readable`

### The complete DjVu encoder suite and doctests passed; NAVM output was also decoded by DjVuLibre when available.

`verified` · `@lege-ecosystem.evidence.auto-toc-djvu-tests/1`

The complete DjVu encoder suite and doctests passed; NAVM output was also decoded by DjVuLibre when available.

**Verifies**

- `completed` `@lege-ecosystem.work.auto-toc-all-outputs/1` — check `djvu-tests`

### All 213 active lege library tests passed (one corpus-dependent test ignored), including TOC, metadata, EPUB, reflow, and outline precedence regressions.

`verified` · `@lege-ecosystem.evidence.auto-toc-lege-lib-tests/1`

All 213 active lege library tests passed (one corpus-dependent test ignored), including TOC, metadata, EPUB, reflow, and outline precedence regressions.

**Verifies**

- `completed` `@lege-ecosystem.work.auto-toc-all-outputs/1` — check `lege-tests`

### All PDF read/write unit and integration tests passed, including source metadata decoding and explicit PDF 1.7 Info dictionary behavior.

`verified` · `@lege-ecosystem.evidence.auto-toc-pdf-read-write-tests/1`

All PDF read/write unit and integration tests passed, including source metadata decoding and explicit PDF 1.7 Info dictionary behavior.

**Verifies**

- `completed` `@lege-ecosystem.work.auto-toc-all-outputs/1` — check `pdf-tests`

### The complete workspace compiled successfully with the automatic TOC and metadata wiring.

`verified` · `@lege-ecosystem.evidence.auto-toc-workspace-check/1`

The complete workspace compiled successfully with the automatic TOC and metadata wiring.

**Verifies**

- `completed` `@lege-ecosystem.work.auto-toc-all-outputs/1` — check `workspace-check`

### All 19 focused TOC tests passed after synthesized page capture was gated by enable_auto_toc.

`verified` · `@lege-ecosystem.evidence.automatic-toc-core-gating/1`

All 19 focused TOC tests passed after synthesized page capture was gated by enable_auto_toc.

**Verifies**

- `superseded` `@lege-ecosystem.work.make-automatic-toc-explicit/1` — check `core-gating`

### All 36 Freya GUI tests passed, including automatic TOC prerequisite normalization, worker flags, and locale schema.

`verified` · `@lege-ecosystem.evidence.automatic-toc-gui-dependencies/1`

All 36 Freya GUI tests passed, including automatic TOC prerequisite normalization, worker flags, and locale schema.

**Verifies**

- `superseded` `@lege-ecosystem.work.make-automatic-toc-explicit/1` — check `gui-dependencies`

### The complete workspace test suite passed after automatic TOC gating and related regression repairs.

`verified` · `@lege-ecosystem.evidence.automatic-toc-workspace-regression/1`

The complete workspace test suite passed after automatic TOC gating and related regression repairs.

**Verifies**

- `superseded` `@lege-ecosystem.work.make-automatic-toc-explicit/1` — check `workspace-regression`

### GPU presentation and layout features compile

`verified` · `@lege-ecosystem.evidence.discrete-gpu-presentation-build/1`

lege-gpu compiled with both presentation and layout-detection enabled after the shared discrete-first policy change.

### Discrete-first adapter policy unit tests pass

`verified` · `@lege-ecosystem.evidence.discrete-gpu-selection-policy-tests/1`

Both deterministic device-type ordering and case-insensitive WGPU_ADAPTER_NAME override matching passed.

### The built Freya executable contained both canonical About documents and the CLI emitted the embedded PP-OCRv5-aware license notice.

`verified` · `@lege-ecosystem.evidence.embedded-about-built-output/1`

The built Freya executable contained both canonical About documents and the CLI emitted the embedded PP-OCRv5-aware license notice.

**Verifies**

- `completed` `@lege-ecosystem.work.embed-freya-about-documents/1` — check `embedded-output`

### All 37 Freya tests passed, including compile-time document presence, current-content assertions, stale cache refresh, and traversal rejection.

`verified` · `@lege-ecosystem.evidence.freya-embedded-about-tests/1`

All 37 Freya tests passed, including compile-time document presence, current-content assertions, stale cache refresh, and traversal rejection.

**Verifies**

- `completed` `@lege-ecosystem.work.embed-freya-about-documents/1` — check `freya-tests`

### The standard Freya processor GUI/worker build and the separate native viewer build both completed successfully.

`verified` · `@lege-ecosystem.evidence.freya-viewer-build-aliases/1`

The standard Freya processor GUI/worker build and the separate native viewer build both completed successfully.

**Verifies**

- `completed` `@lege-ecosystem.work.embed-freya-about-documents/1` — check `gui-builds`

### Bilevel image retention policy tests pass

`verified` · `@lege-ecosystem.evidence.image-detection-policy-tests/1`

Both planned-path image policy regressions and the bounded analysis-surface plan test passed.

### Fresh-process release profiling on Intel Iris Xe/Vulkan showed median first-engine initialization falling from 93.831 ms to 82.591 ms (-11.98%) and median two-session pool initialization from 97.182 ms to 88.091 ms (-9.36%) when full model decoding/preparation overlaps adapter/device discovery. The constructor still joins both jobs before graph allocation and return; adapter selection, fallback ordering, feature requests, buffer allocation, and eager sibling readiness are unchanged.

`verified` · `@lege-ecosystem.evidence.lege-gpu-layout-init-overlap-2026-08-09/1`

Fresh-process release profiling on Intel Iris Xe/Vulkan showed median first-engine initialization falling from 93.831 ms to 82.591 ms (-11.98%) and median two-session pool initialization from 97.182 ms to 88.091 ms (-9.36%) when full model decoding/preparation overlaps adapter/device discovery. The constructor still joins both jobs before graph allocation and return; adapter selection, fallback ordering, feature requests, buffer allocation, and eager sibling readiness are unchanged.

**Verifies**

- `superseded` `@lege-ecosystem.lege-process.about-gpu-init-2026-08-09/2` — check `gpu-initialization-evaluated`

### The committed optimization matches the measured release implementation: median first-engine initialization decreased from 93.831 ms to 82.591 ms (-11.98%) and two-session startup from 97.182 ms to 88.091 ms (-9.36%). Model preparation and unchanged adapter/device discovery join before eager graph allocation and return.

`verified` · `@lege-ecosystem.evidence.lege-gpu-layout-init-overlap-committed-2026-08-09/1`

The committed optimization matches the measured release implementation: median first-engine initialization decreased from 93.831 ms to 82.591 ms (-11.98%) and two-session startup from 97.182 ms to 88.091 ms (-9.36%). Model preparation and unchanged adapter/device discovery join before eager graph allocation and return.

**Verifies**

- `completed` `@lege-ecosystem.lege-process.about-gpu-init-2026-08-09/3` — check `gpu-initialization-evaluated`

### Both shipped About popup implementations now construct only their header when the theme chooser is closed, so no removed-row child or spacing remains. The email field/value is absent from both GUI schemas and both locale files, the scoped search returned no matches, and both locale schema tests passed.

`verified` · `@lege-ecosystem.evidence.lege-process-about-email-removed-2026-08-09/1`

Both shipped About popup implementations now construct only their header when the theme chooser is closed, so no removed-row child or spacing remains. The email field/value is absent from both GUI schemas and both locale files, the scoped search returned no matches, and both locale schema tests passed.

**Verifies**

- `superseded` `@lege-ecosystem.lege-process.about-gpu-init-2026-08-09/2` — check `about-email-removed`

### At the committed active-work revision, the scoped search found no About email field, value, or use, and both shipped GUI locale schema tests passed.

`verified` · `@lege-ecosystem.evidence.lege-process-about-email-removed-committed-2026-08-09/1`

At the committed active-work revision, the scoped search found no About email field, value, or use, and both shipped GUI locale schema tests passed.

**Verifies**

- `completed` `@lege-ecosystem.lege-process.about-gpu-init-2026-08-09/3` — check `about-email-removed`

### At the committed active-work revision, both GUI locale tests, hardware WGPU constant/pipeline sharing test, and affected package checks passed.

`verified` · `@lege-ecosystem.evidence.lege-process-about-gpu-checks-committed-2026-08-09/1`

At the committed active-work revision, both GUI locale tests, hardware WGPU constant/pipeline sharing test, and affected package checks passed.

**Verifies**

- `completed` `@lege-ecosystem.lege-process.about-gpu-init-2026-08-09/3` — check `focused-tests-pass`

### Touched Rust modules passed focused formatting, both GUI locale tests passed, the GPU sharing test passed on hardware WGPU, Lege and lege-gpu checks passed, and the patch has no whitespace errors. Existing dependency and legacy-code warnings remain non-fatal.

`verified` · `@lege-ecosystem.evidence.lege-process-about-gpu-focused-checks-2026-08-09/1`

Touched Rust modules passed focused formatting, both GUI locale tests passed, the GPU sharing test passed on hardware WGPU, Lege and lege-gpu checks passed, and the patch has no whitespace errors. Existing dependency and legacy-code warnings remain non-fatal.

**Verifies**

- `superseded` `@lege-ecosystem.lege-process.about-gpu-init-2026-08-09/2` — check `focused-tests-pass`

### Linux GUI backend policy tests

`verified` · `@lege-ecosystem.evidence.linux-gui-backend-policy-tests/1`

All four Linux backend-selection tests passed: reachable Wayland preference, stale Wayland fallback to X11, single-backend selection, and exhausted-backend diagnostics.

**Verifies**

- `completed` `@lege-ecosystem.work.linux-gui-backend-fallback/1` — check `backend-policy`

### Lege Freya GUI build

`verified` · `@lege-ecosystem.evidence.linux-gui-freya-build/1`

The complete Lege Freya GUI compiled successfully against the patched freya-winit crate.

**Verifies**

- `completed` `@lege-ecosystem.work.linux-gui-backend-fallback/1` — check `compile`

### Stale Wayland environment falls back to X11

`verified` · `@lege-ecosystem.evidence.linux-gui-stale-wayland-fallback/1`

With WAYLAND_DISPLAY pointed at a nonexistent socket and DISPLAY=:0 live, target/debug/lege-gui remained running until a five-second timeout instead of panicking; with all display variables removed it exited immediately with the specific exhausted-backends diagnostic.

**Verifies**

- `completed` `@lege-ecosystem.work.linux-gui-backend-fallback/1` — check `startup-diagnostic`

### Native PDF agent tool plan written

`verified` · `@lege-ecosystem.evidence.native-pdf-agent-tool-plan/1`

The plan documents the existing crate foundation, measured baseline, agent protocol, bounded JSON/JSONL contract, phased implementation, limitations, and validation criteria.

**Verifies**

- `completed` `@lege-ecosystem.work.about-image-pdf-agent-tool/1` — check `agent-tool-plan`

### OCR CLI lint

`verified` · `@lege-ecosystem.evidence.ocr-foundation-clippy-cli-2026-08-11/1`

The new production CLI target passed clippy with warnings denied.

**Verifies**

- `completed` `@lege-ecosystem.work.ocr-product-foundation/2` — check `production-lints`

### OCR production library lint

`verified` · `@lege-ecosystem.evidence.ocr-foundation-clippy-libs-2026-08-11/1`

The new production library targets passed clippy with warnings denied.

**Verifies**

- `completed` `@lege-ecosystem.work.ocr-product-foundation/2` — check `production-lints`

### OCR foundation test suite

`verified` · `@lege-ecosystem.evidence.ocr-foundation-tests-2026-08-11/1`

All DocIR, batch, export, pipeline, CLI, PDF intake, and OCR library tests passed, including resume and searchable-PDF text extraction.

**Verifies**

- `completed` `@lege-ecosystem.work.ocr-product-foundation/2` — check `foundation-tests`

### GPU scheduler and compact OCR backend tests

`verified` · `@lege-ecosystem.evidence.ocr-gpu-scheduler-specialists-2026-08-11/1`

All lege-ocr tests and 61 lege-gpu tests passed (one manual benchmark ignored), including compact CTC decode parity and the real WGPU top-two reduction test.

**Verifies**

- `superseded` `@lege-ecosystem.work.ocr-backend-scheduler/3` — check `backend-tests`
- `active` `@lege-ecosystem.work.ocr-backend-scheduler/4` — check `backend-tests`

### All 35 Freya tests passed, including Fast, Thorough, and disabled OCR automatic-TOC argument coverage.

`verified` · `@lege-ecosystem.evidence.ocr-owned-toc-gui-suite/1`

All 35 Freya tests passed, including Fast, Thorough, and disabled OCR automatic-TOC argument coverage.

**Verifies**

- `completed` `@lege-ecosystem.work.fold-automatic-toc-into-ocr/2` — check `gui-behavior`

### English and German locale JSON parsed and the bundled-locale GUI schema test passed.

`verified` · `@lege-ecosystem.evidence.ocr-owned-toc-locales/1`

English and German locale JSON parsed and the bundled-locale GUI schema test passed.

**Verifies**

- `completed` `@lege-ecosystem.work.fold-automatic-toc-into-ocr/2` — check `locale-schema`

### OCR pipeline specialist integration tests

`verified` · `@lege-ecosystem.evidence.ocr-pipeline-specialist-integration-2026-08-11/1`

Focused workspace tests passed across GPU/OCR, DocIR, pipeline, export, and CLI, including scheduler bounds, model-pack checksums, table spans, formula cleanup, structured-region merge, resume, and multi-worker batch behavior.

### OCR production targets strict Clippy

`verified` · `@lege-ecosystem.evidence.ocr-production-clippy-2026-08-11/1`

All selected OCR production libraries and CLI binary passed Clippy with warnings denied.

### Mixed eligible and maskless pages process through independent paths

`verified` · `@lege-ecosystem.evidence.preserved-jbig2-mixed-page-fallback/1`

The eligible first page retained its 2222x3191 56 KiB source stencil, the maskless second page produced the existing 836x1200 fallback stencil, and both rendered nonblank.

**Verifies**

- `completed` `@lege-ecosystem.work.preserve-jbig2-smask-text-plane/1` — check `mixed-page-fallback`

### Preserved mask path is PDF-only, guarded, and bypasses authoritative rethresholding

`verified` · `@lege-ecosystem.evidence.preserved-jbig2-scope-and-bypass-audit/1`

Source inspection confirmed entry exists only in the non-reflow PDF grayscale planned path; unsupported placement/crop/rotation, non-SMask mask forms, malformed decode, and ineligible pages return per-page fallback. Eligible cleanup and both OCR modes use source-derived coverage/binary guidance, final composition passes original bytes, and figure crops overlay afterward.

**Verifies**

- `completed` `@lege-ecosystem.work.preserve-jbig2-smask-text-plane/1` — check `scope-guards`

### Preserved JBIG2 SMask focused tests pass

`verified` · `@lege-ecosystem.evidence.preserved-jbig2-smask-focused-tests/1`

Renderer decode/polarity/malformed fallback, strict page eligibility including globals and exclusions, Lanczos fractional coverage, writer stencil polarity/globals, continuous background whitening, integration compilation, and all read/write tests passed.

**Verifies**

- `completed` `@lege-ecosystem.work.preserve-jbig2-smask-text-plane/1` — check `eligibility-corpus`
- `completed` `@lege-ecosystem.work.preserve-jbig2-smask-text-plane/1` — check `lanczos-working-plane`

### Before canonical edits, project, Linux staging, and MSIX documentation.html copies were byte-identical at SHA-256 c78277275d80915433e541dc2e712d18281dd19c7a855b66e0145929d84c9e5a and all contained the Reflow section.

`verified` · `@lege-ecosystem.evidence.release-document-copy-audit/1`

Before canonical edits, project, Linux staging, and MSIX documentation.html copies were byte-identical at SHA-256 c78277275d80915433e541dc2e712d18281dd19c7a855b66e0145929d84c9e5a and all contained the Reflow section.

**Verifies**

- `completed` `@lege-ecosystem.work.embed-freya-about-documents/1` — check `document-audit`

### RTX 4060 exists but is not Vulkan-visible in this environment

`verified` · `@lege-ecosystem.evidence.rtx4060-vulkan-visibility/1`

nvidia-smi saw the RTX 4060, while wgpu enumerated only Intel Iris Xe and llvmpipe. Vulkan loader diagnostics showed the NVIDIA ICD could not load libGLX_nvidia.so.0 because only an i386 library was installed.

### Structures pages 190–210 retain all detected illustrations

`verified` · `@lege-ecosystem.evidence.structures-image-overlay-range/1`

The 21-page output had two image objects (CCITT base plus JP2 overlay) exactly on source pages 195, 197, 198, 199, 202, 204, and 208; every other page had only the CCITT base.

**Verifies**

- `completed` `@lege-ecosystem.work.about-image-pdf-agent-tool/1` — check `structures-diagnosed`

### Structures output stencil exactly preserves source opacity support

`verified` · `@lege-ecosystem.evidence.structures-jbig2-ink-support-oracle/1`

After applying the required SMask-to-ImageMask polarity inversion, ImageMagick reported zero differing pixels across the full 2222x3191 mask grid, preserving ink support and connected-component topology.

**Verifies**

- `completed` `@lege-ecosystem.work.preserve-jbig2-smask-text-plane/1` — check `quality-oracle`

### Structures native JBIG2 stream survives processing byte-identically

`verified` · `@lege-ecosystem.evidence.structures-jbig2-payload-roundtrip/1`

Input SMask object 1277 and output stencil object 3 were both 2222x3191 and had identical SHA-256 13101f05e92f2f0c62d6014dfd14cb7f617ddabfa1febf5013e7ef472cd8fe10.

### Structures native JBIG2 stream byte-identical roundtrip (corrected hash)

`verified` · `@lege-ecosystem.evidence.structures-jbig2-payload-roundtrip-corrected/1`

Source object 1277, no-layout output object 3, and layout-enabled output object 3 all yielded SHA-256 7ed6f9593e753dd7c7e9ac77a9569051c702dc25337b4d8775effc76378e0050. This corrects a stale hash transcribed into the earlier payload-roundtrip evidence note; the observed equality result is unchanged.

**Verifies**

- `completed` `@lege-ecosystem.work.preserve-jbig2-smask-text-plane/1` — check `payload-roundtrip`

### Layout detection composes figures without replacing native text mask

`verified` · `@lege-ecosystem.evidence.structures-layout-preserved-mask-composition/1`

Layout inference ran and emitted a separate RGB figure overlay while output retained the native 2222x3191 JBIG2 stencil; its stream SHA-256 exactly matched source object 1277 (7ed6f9593e753dd7c7e9ac77a9569051c702dc25337b4d8775effc76378e0050 in this post-build run).

**Verifies**

- `completed` `@lege-ecosystem.work.preserve-jbig2-smask-text-plane/1` — check `pipeline-bypass`

### After restoring the five legacy reports still cited by active migration trackers, strict validation reported zero AKR-M022 diagnostics; the count fell from 153 to zero, leaving only four AKR-G012 ancestry warnings.

`verified` · `@lege-ecosystem-perf.evidence.akr-m022-cleanup-2026-08-09/1`

After restoring the five legacy reports still cited by active migration trackers, strict validation reported zero AKR-M022 diagnostics; the count fell from 153 to zero, leaving only four AKR-G012 ancestry warnings.

**Verifies**

- `superseded` `@lege-ecosystem-perf.akr.papercut-cleanup-2026-08-09/1` — check `legacy-source-validation`

### At the committed active cleanup revision, strict validation output contains no AKR-M022 missing legacy-source diagnostics.

`verified` · `@lege-ecosystem-perf.evidence.akr-m022-cleanup-committed-2026-08-09/1`

At the committed active cleanup revision, strict validation output contains no AKR-M022 missing legacy-source diagnostics.

**Verifies**

- `completed` `@lege-ecosystem-perf.akr.papercut-cleanup-2026-08-09/3` — check `legacy-source-validation`

### Strict validation output contains no AKR-M022 missing legacy-source diagnostics after the restored historical sources were committed.

`verified` · `@lege-ecosystem-perf.evidence.akr-m022-cleanup-revalidated-2026-08-09/1`

Strict validation output contains no AKR-M022 missing legacy-source diagnostics after the restored historical sources were committed.

**Verifies**

- `superseded` `@lege-ecosystem-perf.akr.papercut-cleanup-2026-08-09/2` — check `legacy-source-validation`

### Direct-output release benchmark smoke

`verified` · `@lege-ecosystem-perf.evidence.jp2-direct-output-benchmark-smoke-2026-08-09/1`

The release benchmark exercised path=session_packed_into and produced stable output hash 08ffafc12705f1c4 at 2.993 ms median for the synthetic 512x512 Gray8 fixture.

### JP2 direct-output codec regression suite

`verified` · `@lege-ecosystem-perf.evidence.jp2-direct-output-codec-tests-2026-08-09/1`

The jp2lam library suite passed: 340 passed, 0 failed, 5 ignored, including direct-write single-tile and reduced multi-tile stride-padding coverage.

### Renderer image codec regression suite

`verified` · `@lege-ecosystem-perf.evidence.jp2-direct-output-renderer-tests-2026-08-09/1`

All pdf-image tests passed, including 7 JPX codec integration tests covering packed Gray/RGB, reduced output, high-bit-depth fallback, limits, and geometry.

### Ecosystem workspace check after JP2 direct output

`verified` · `@lege-ecosystem-perf.evidence.jp2-direct-output-workspace-check-2026-08-09/1`

The complete Lege ecosystem workspace compiled successfully after routing packed JPX output directly into the renderer's final Arc allocation.

### JPEG direct-Arc decoder assembly profile

`verified` · `@lege-ecosystem-perf.evidence.jpeg-direct-arc-decode-profile/2`

The representative JPEG decode-only median fell from 81.430 ms to 63.842 ms (-21.6%) after assembly moved from Vec-to-Arc copy to direct Arc output, with output hash 3dfafb56edf8d303 unchanged.

**supersedes** `@lege-ecosystem-perf.evidence.jpeg-direct-arc-decode-profile/1`

### JPEG Sweep output-copy removal profile

`verified` · `@lege-ecosystem-perf.evidence.jpeg-sweep-final-buffer-profile/2`

The final 11-run JPEG Sweep compiled-page median was 95.828 ms versus the 7-run 161.684 ms baseline (-40.7%); render.output fell from 63.884 ms to 0.000270 ms, peak live renderer bytes halved, and output hash 3dfafb56edf8d303 was unchanged.

**supersedes** `@lege-ecosystem-perf.evidence.jpeg-sweep-final-buffer-profile/1`

### Renderer, codec, scheduler, postprocess, and chaos regression suite passes

`verified` · `@lege-ecosystem-perf.evidence.mrc-smask-final-buffer-regression-suite/2`

The full targeted renderer/codec test set passed after the Arc final-buffer handoff, including pixel-equivalence, fixture, and chaos coverage; focused RGB8 area-min mask tests also passed 6/6.

**supersedes** `@lege-ecosystem-perf.evidence.mrc-smask-final-buffer-regression-suite/1`

### The documented renderer clippy gate exited successfully after replacing expect-based fixed-size annotation conversion and handling a theoretically shared JPX Arc with a typed decode error. pdf-document tests and all 74 pdf-image tests also passed.

`verified` · `@lege-ecosystem-perf.evidence.renderer-expect-used-cleanup-2026-08-09/1`

The documented renderer clippy gate exited successfully after replacing expect-based fixed-size annotation conversion and handling a theoretically shared JPX Arc with a typed decode error. pdf-document tests and all 74 pdf-image tests also passed.

**Verifies**

- `superseded` `@lege-ecosystem-perf.akr.papercut-cleanup-2026-08-09/1` — check `renderer-clippy-expect-gate`

### At the committed active cleanup revision, the documented renderer clippy gate completes successfully without an expect-used error.

`verified` · `@lege-ecosystem-perf.evidence.renderer-expect-used-cleanup-committed-2026-08-09/1`

At the committed active cleanup revision, the documented renderer clippy gate completes successfully without an expect-used error.

**Verifies**

- `completed` `@lege-ecosystem-perf.akr.papercut-cleanup-2026-08-09/3` — check `renderer-clippy-expect-gate`

### The documented renderer clippy gate completed successfully at the current cleanup-record commit; warnings remain but no expect-used error is present.

`verified` · `@lege-ecosystem-perf.evidence.renderer-expect-used-cleanup-revalidated-2026-08-09/1`

The documented renderer clippy gate completed successfully at the current cleanup-record commit; warnings remain but no expect-used error is present.

**Verifies**

- `superseded` `@lege-ecosystem-perf.akr.papercut-cleanup-2026-08-09/2` — check `renderer-clippy-expect-gate`

### Structures.pdf direct JPX output profile

`verified` · `@lege-ecosystem-perf.evidence.structures-direct-output-profile-2026-08-09/1`

On Structures.pdf page 0, 21-run direct-output medians were 246.834 ms decode-only, 204.616 ms JPX, and 41.516 ms JBIG2 with unchanged hash fdc3994f80330ee0; JPX p25/p75 were 195.466/233.428 ms.

### Structures.pdf warm renderer profile after direct JPX output

`verified` · `@lege-ecosystem-perf.evidence.structures-direct-output-warm-profile-2026-08-09/1`

Structures.pdf page 0 stayed byte-identical at hash fdc3994f80330ee0; 11-run medians were 70.697 ms compiled and 70.049 ms warm-decoded, with render.output approximately 0.000201 ms.

### Structures.pdf direct Arc output versus owned Vec control

`verified` · `@lege-ecosystem-perf.evidence.structures-direct-vs-owned-output-2026-08-09/2`

A corrected 21-run control temporarily restored the former packed owned-raster plus Vec-to-Arc path. It measured 196.892 ms median JPX (p25/p75 192.961/205.569), versus 204.616 ms (195.466/233.428) for direct Arc output in a sequential same-session comparison. Direct output was about 3.9% slower at the median, and the distributions overlap, so this run demonstrates no throughput improvement. Both output hashes were fdc3994f80330ee0. The direct path still removes one full-image allocation and copy; the temporary control branch was removed after measurement. Revision 1 used the native-planar fallback as its control and is superseded by this corrected packed-path measurement.

**supersedes** `@lege-ecosystem-perf.evidence.structures-direct-vs-owned-output-2026-08-09/1`

### Structures.pdf soft-mask renderer and decoder follow-up profile

`verified` · `@lege-ecosystem-perf.evidence.structures-smask-followup-profile/2`

On the supplied Structures.pdf page 0 at scale 2.0833333 (926x1330 output; 2222x3191 JPX sources), 11-run medians were 71.028 ms compiled and 70.449 ms warm-decoded with hash fdc3994f80330ee0; render.output was approximately 0.00017 ms. Decode-only was 237.823 ms: 196.440 ms JPX plus 41.367 ms JBIG2, confirming cold decode remains outside the renderer-side fast path.

**supersedes** `@lege-ecosystem-perf.evidence.structures-smask-followup-profile/1`

### Tier-1 parallel decode reworked to weighted LPT scheduling + per-worker persistent Tier1Scratch (working-tree change to src/decode/t1.rs, uncommitted at this HEAD); full lib suite 339 passed incl. serial_and_budgeted_tier1_are_byte_identical.

`verified` · `@lege-ecosystem-perf.evidence.tier1-lpt-scheduling/1`

Tier-1 parallel decode reworked to weighted LPT scheduling + per-worker persistent Tier1Scratch (working-tree change to src/decode/t1.rs, uncommitted at this HEAD); full lib suite 339 passed incl. serial_and_budgeted_tier1_are_byte_identical.
