# lege-pdf/render — placeholder

The pdf-renderer workspace stays at
`/mnt/Samsung980_1TB/Rust-projects/pdfium-port-plan/pdf-renderer` while its
integration and text-extraction work continues. Do not copy or move it here.
This directory is intentionally empty and excluded from the Lege workspace.

Lege reaches the external crates only through `../read` (`lege-pdf-read`).
The temporary paths are declared once in the root workspace manifest.
