# Rust build artifacts
/target
Cargo.lock
**/*.rs.bk

# IDE
.idea/
.vscode/
*.swp
*.swo
*~
.DS_Store
.claude/

# Development and analysis
*.md
!AGENTS.md
!README.md
!CHANGELOG.md
!jp2lam-photo-hdr-readiness-plan.md
!real-estate-hdr-format-recommendations.md
# AKR migration trackers still cite these legacy reports. Keep them until the
# corresponding import work is fully dispositioned and completed.
!decode-fix-plan/*.md
# Stands in for an AKR observation that could not be written while the ledger is
# invalid (see the file's own "Why this is a file" section). It carries a real
# open decision, so it must not be ignored along with the scratch notes. Delete
# it, and this line, once the record is proposed.
!llm-docs/TAUBMAN_MASKING_UNWIRED.md
*.pdf
*.txt
perf.data*
flamegraph*.svg
flamegraph_analysis.md

# Test/build artifacts
*.jp2
*.j2k
*.png
