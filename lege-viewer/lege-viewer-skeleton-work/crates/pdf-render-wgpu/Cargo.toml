[package]
name = "pdf-render-wgpu"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

# Phase 7+ (roadmap §9). Deliberately has NO wgpu dependency yet: the crate
# exists so the workspace dependency direction is enforced from day one and
# scheduler fallback-policy code has a second backend to route around.
[dependencies]
thiserror = { workspace = true }
pdf-page-ir = { workspace = true }
pdf-render-api = { workspace = true }

[lints]
workspace = true
